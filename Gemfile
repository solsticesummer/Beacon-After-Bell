source "https://rubygems.org"

# Optional, for local preview only (`bundle install && bundle exec
# jekyll serve`). GitHub Pages builds the site remotely on every push
# regardless of whether this is installed — see README.md.
gem "github-pages", group: :jekyll_plugins
